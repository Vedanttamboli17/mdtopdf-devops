apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    # Reachable on the compose network by service name
    url: http://prometheus:9090
    isDefault: true
    editable: true
